zuul:
  routes:
  # /myProduct/product/list -> /product/product/list
    aaaaaa:
      myProduct: /myProduct/**
      serviceId: product
      #将敏感头不过滤(cookies之类的)
      sensitiveHeaders:
  #简洁写法
#    product: /myProduct/**
  #排除某些路由
  ignored-patterns:
    - /**/product/listForOrder
management:
  security:
    enabled: false