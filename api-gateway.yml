zuul:
  routes:
  # /myProduct/product/list -> /product/product/list
    myProduct:
      myProduct: /myProduct/**
      serviceId: product
  #简洁写法
  #    product: /myProduct/**
  #排除某些路由
  ignored-patterns:
    - /**/product/listForOrder
  #将敏感头不过滤(cookies之类的)
  sensitive-headers:
management:
  security:
    enabled: false